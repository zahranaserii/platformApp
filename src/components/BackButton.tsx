import { Button } from "antd";
import { unstable_HistoryRouter } from "react-router-dom";
const BackButton = () => {
  //hooks
  //function
  const goBack = () => {};
  return <Button onClick={goBack}>صفحه قبلی</Button>;
};

export default BackButton;
